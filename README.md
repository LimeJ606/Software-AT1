# Software-AT1
To run the PWA, extract the zip and open it in vs code.
Run the command 'node node.js' in a git bash terminal.
Use the url provided in the terminal to access the PWA.

Functions:
You can use the nav bar at the bottom to switch between pages.
You can click the images of jars to change the theme.
